/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co.com",
      "example.com",
      "upload.wikimedia.org",
      "i.chaldn.com",
    ],
  },
};

export default nextConfig;
